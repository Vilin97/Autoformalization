# Work Plan — Grothendieck Vanishing

**Updated**: 2026-04-03T09:05Z

## Status Summary
- **CI**: In progress (prev 2 green)
- **Sorry count**: 3 (1 in SheafHom.lean:63, 2 in FiniteGeneratorReduction.lean:180,188,237)
- **Files**: 14 main files, ~6000 lines total
- **Docs**: Blueprint 404 (deployed on main only)

## Active Multi-Cycle Strategies

1. **Close AB5 sorry (isSheaf_filtered_colimit_of_sheaves)**: This is the core mathematical gap. Filtered presheaf colimits of sheaves are sheaves on Noetherian spaces. Approach: use `Sheaf.ab5ofSize` or prove directly from Noetherian → finite covers → finite limits commute with filtered colimits. Submitted to Aristotle (job PENDING_SUBMISSION). Working on it manually in parallel.

2. **Restructure hQprov induction**: Sorry #2 (line 188) has a suspicious recursive structure. The `hQvan_provider` asks for vanishing on quotients-of-quotients, which nests injective presentations. Consider whether the outer dimension induction already provides what's needed, making the sorry trivially closeable from the caller's context.

## This Cycle's Work Items

1. **[/prove] Fix documentation line numbers** (P2, immediate)
   CLAUDE.md and main.lean cite wrong line numbers for sorry's #2 and #3. Fix:
   - CLAUDE.md: 146→188, 195→237
   - main.lean:11-12: same corrections

2. **[/prove] Attempt to close sorry #3 (n'=0 dead case, line 237)** (P1)
   The n'=0 case in `ext_comm_filtered_colimit_mono` is claimed dead at the call site (dim ≥ 1 ⟹ m ≥ 2 ⟹ n ≥ 1). Two approaches:
   - Add `(hn : n ≥ 1)` or `(hn : n ≠ 0)` hypothesis and propagate through callers
   - Or prove directly: at n'=0, need `Subsingleton (Ext^0(Z, Q_j))` = `Subsingleton (Hom(Z, Q_j))`. This requires showing Q_j is zero or Hom(Z, Q_j) = 0.
   If neither works, at least replace the sorry with `absurd` + a proof that n' ≠ 0.

3. **[/prove] Attempt to close sorry #2 (hQprov, line 188)** (P1)
   The key insight: `hQprov` at degree n'+1 asks for `Ext^{n''}(Z, cokernel(Qcocone.ι.app j ≫ ip'.shortComplex.f))` where n'' = n'. This is vanishing at the SAME degree on a DIFFERENT object. The outer caller (`ext_comm_filtered_colimit_mono`) has `hvan : ∀ j, Subsingleton (Ext Z (Y.obj j) n)` where n = n'+2 (since we're in the succ case). Can we construct the needed vanishing from the LES + hvan?

4. **[/submit-aristotle] Submit isSheaf_filtered_colimit_of_sheaves** (P1)
   Prepare standalone file for Aristotle. The statement is clean: filtered presheaf colimits of sheaves are sheaves when the topology is generated by finite covers (Noetherian).

5. **[/simplify] Fix line number documentation lies** (P2)
   Update CLAUDE.md and main.lean with correct sorry line numbers.

## Backlog
- P1: Fix docs/blueprint 404 (needs merge to main or deploy workflow change)
- P2: Split IrreducibleStep.lean (1264 lines → extract SheafStalkAlgebra section)
- P3: ZeroOutside.lean (733 lines), SheafStalkAlgebra.lean (688 lines), FlasqueVanishing.lean (616 lines) — all over 600-line guideline
- P3: Clean up `finsetCoproductIncl` vs `finsetCoproductInclGen` naming
- P4: Upstream FlasqueVanishing, ext_sandwich, sheafH_vanishing_cascade to Mathlib
