import Mathlib

/-!
  GeneratedSubsheaf.lean — Generic coproduct-image API for generated subsheaves

  Provides:
  - `TopCat.Sheaf.familyMap`: universal map from a family coproduct into a sheaf
  - `TopCat.Sheaf.familyImage`: image sheaf generated by that family
  - `TopCat.Sheaf.familyImageι`: canonical inclusion of the generated image
  - `TopCat.Sheaf.familyImage_ι_eq`: definitional API lemma for the inclusion
  - `TopCat.Presheaf.SectionIndex` / `TopCat.Sheaf.SectionIndex`: generic indexing types
    for local sections
-/

universe u

open CategoryTheory TopologicalSpace Limits Opposite

noncomputable section

namespace TopCat

namespace Sheaf

/-- For a family of morphisms into `F`, the universal map from their coproduct into `F`. -/
abbrev familyMap {C : Type*} [Category C] {X : TopCat.{u}} {ι : Type*}
    (G : ι → Sheaf C X) {F : Sheaf C X} (f : ∀ i, G i ⟶ F)
    [HasCoproduct G] :
    (∐ G) ⟶ F :=
  Sigma.desc f

/-- The image subsheaf generated by a family of morphisms into `F`. -/
abbrev familyImage {C : Type*} [Category C] {X : TopCat.{u}} {ι : Type*}
    (G : ι → Sheaf C X) {F : Sheaf C X} (f : ∀ i, G i ⟶ F)
    [HasCoproduct G] [HasImage (familyMap G f)] :
    Sheaf C X :=
  Limits.image (familyMap G f)

/-- The canonical inclusion of the image generated by a family of morphisms into `F`. -/
abbrev familyImageι {C : Type*} [Category C] {X : TopCat.{u}} {ι : Type*}
    (G : ι → Sheaf C X) {F : Sheaf C X} (f : ∀ i, G i ⟶ F)
    [HasCoproduct G] [HasImage (familyMap G f)] :
    familyImage G f ⟶ F :=
  Limits.image.ι (familyMap G f)

theorem familyImage_ι_eq {C : Type*} [Category C] {X : TopCat.{u}} {ι : Type*}
    (G : ι → Sheaf C X) {F : Sheaf C X} (f : ∀ i, G i ⟶ F)
    [HasCoproduct G] [HasImage (familyMap G f)] :
    familyImageι G f = Limits.image.ι (familyMap G f) := rfl

end Sheaf

namespace Presheaf

/-- Indexing type for all local sections of a presheaf. -/
abbrev SectionIndex {C : Type*} [Category C] {FC : C → C → Type*} {CC : C → Type*}
    [∀ X Y, FunLike (FC X Y) (CC X) (CC Y)] [ConcreteCategory C FC] {X : TopCat.{u}}
    (F : TopCat.Presheaf C X) :=
  Σ U : Opens X, ToType (F.obj (op U))

end Presheaf

namespace Sheaf

/-- Indexing type for all local sections of a sheaf. -/
abbrev SectionIndex {C : Type*} [Category C] {FC : C → C → Type*} {CC : C → Type*}
    [∀ X Y, FunLike (FC X Y) (CC X) (CC Y)] [ConcreteCategory C FC] {X : TopCat.{u}}
    (F : Sheaf C X) :=
  TopCat.Presheaf.SectionIndex F.presheaf

end Sheaf

end TopCat
