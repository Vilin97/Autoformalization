Pivot away from `exists_section_generating_stalks` — it has been golfed in 5 cycles and remaining transport is dense but load-bearing. Target the next-largest file: `CohomologyAPI.lean` (782 raw LOC). Scan for `(F : Presheaf X) (hF : F.IsSheaf)` triples that should reduce to a single `Sheaf` argument (Principle 2), or pick the longest proof body in that file via the audit's Phase-4 list.
