Pivot off `sheafH_filtered_colimit_comparison_one_iso_hom` (now 3 successive shrinking-yield passes since cycle 47). Try `sheafH_filtered_colimit_comparison_succ_compatibility` (PresheafFilteredColimitCore.lean:428, body=53) or another long-proof from the audit — IrreducibleStep `exists_section_generating_stalks` is also down to body=106 but has produced 2 recent passes already.
