Continue Phase 4 unused-decl cleanup: investigate the remaining `@[simps]` on `zeroOutside` (lines 213/228 still reference `zeroOutside_obj`/`zeroOutside_map`) — see whether those references can be replaced by direct unfolds and the attribute removed. Otherwise, pivot to scanning CohomologyAPI.lean (largest file, 609 normalized LOC) for `(F : Presheaf X) (hF : F.IsSheaf)` triples to collapse to `Sheaf` arguments per Principle 2.
