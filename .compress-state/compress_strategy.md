Pivot to a fresh long-proof target. Best candidates: the largest body in `CohomologyAPI.lean` (782 raw LOC, untouched recently) or `filtered_colimit_exists_compatible_representatives` in `PresheafFilteredColimitGeneral.lean` (still 55-line body after cycle 67). Avoid re-mining `exists_section_generating_stalks`, `closedIncl_unit_stalk_isIso`, `sheafH_filtered_colimit_comparison_one_iso_hom`, and `finsetGeneratedSheaf_vanishing` — all show diminishing returns.
