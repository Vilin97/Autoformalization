No body ≥50-line proofs remain. Pivot to Principle 2 (Sheaf-reversion) on `CohomologyAPI.lean`'s repeated `(F₁ F₂ F₃ : Presheaf) (h₁ h₂ h₃ : IsSheaf)` blocks (largest file at 782 LOC, ~192-273 line potential). Alternatively re-run the audit to check for newly-unreachable decls after the simplifications.
