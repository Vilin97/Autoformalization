Continue Phase-2 unused-decl deletion in CohomologyAPI.lean. Next candidate: `sheafCohomologyFunctor_map_succ_of_map_eq_presheaf` (rg already finds no occurrences, so the checklist item may already pass — verify and flip). After that, deleting `sheafCohomologyFunctor_obj` requires editing call sites in FinitelyGeneratedVanishing.lean:130 and ClosedImmersionCohomology.lean:157, so plan it as a dedicated cycle.
